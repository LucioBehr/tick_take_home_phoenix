defmodule TickTakeHomeWeb.TickTakeHomeController do
  use TickTakeHomeWeb, :controller

  alias TickTakeHome, as: Server

  # def create_user(conn, %{"wallet_id" => wallet_id}) do
  #   case Server.create_user(wallet_id) do
  #     {:error, _} -> {:error, error}
  #     {:ok, user} -> {:ok, user}

  #   end
  #end
end
